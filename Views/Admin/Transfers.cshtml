@model List<Transaction>
@{
    ViewData["Title"] = "Fund Transfers";
    ViewBag.SectionTitle = "Fund Transfers";
    var nameMap = (Dictionary<string, string>)ViewBag.NameMap;
}

<div class="mb-8">
    <h1 class="text-2xl font-bold text-slate-900">Fund Transfer Management</h1>
    <p class="mt-1 text-sm text-slate-600">All fund transfers across the system (sender-side records).</p>
</div>

<div class="rounded-lg bg-white p-6 shadow">
    <partial name="_TransactionFilters" />

    <div class="mt-4 overflow-x-auto">
        <table class="min-w-full divide-y divide-slate-200">
            <thead class="bg-slate-50">
                <tr>
                    <th class="px-4 py-3 text-left text-xs font-semibold uppercase tracking-wider text-slate-500">Transfer ID</th>
                    <th class="px-4 py-3 text-left text-xs font-semibold uppercase tracking-wider text-slate-500">Sender Account</th>
                    <th class="px-4 py-3 text-left text-xs font-semibold uppercase tracking-wider text-slate-500">Sender Name</th>
                    <th class="px-4 py-3 text-left text-xs font-semibold uppercase tracking-wider text-slate-500">Receiver Account</th>
                    <th class="px-4 py-3 text-left text-xs font-semibold uppercase tracking-wider text-slate-500">Receiver Name</th>
                    <th class="px-4 py-3 text-right text-xs font-semibold uppercase tracking-wider text-slate-500">Amount</th>
                    <th class="px-4 py-3 text-left text-xs font-semibold uppercase tracking-wider text-slate-500">Date &amp; Time</th>
                    <th class="px-4 py-3 text-left text-xs font-semibold uppercase tracking-wider text-slate-500">Status</th>
                    <th class="px-4 py-3 text-left text-xs font-semibold uppercase tracking-wider text-slate-500">Reference</th>
                    <th class="px-4 py-3 text-left text-xs font-semibold uppercase tracking-wider text-slate-500">Actions</th>
                </tr>
            </thead>
            <tbody class="divide-y divide-slate-200 bg-white">
                @if (Model.Count == 0)
                {
                    <tr>
                        <td colspan="10" class="px-4 py-8 text-center text-sm text-slate-500">No transfers found.</td>
                    </tr>
                }
                @foreach (var t in Model)
                {
                    <tr class="hover:bg-slate-50">
                        <td class="px-4 py-3 text-sm text-slate-600">@t.Id.Substring(t.Id.Length - 8)</td>
                        <td class="px-4 py-3 text-sm font-medium text-slate-900">@t.AccountNumber</td>
                        <td class="px-4 py-3 text-sm text-slate-600">@(nameMap.ContainsKey(t.AccountNumber) ? nameMap[t.AccountNumber] : "—")</td>
                        <td class="px-4 py-3 text-sm font-medium text-slate-900">@t.RelatedAccountNumber</td>
                        <td class="px-4 py-3 text-sm text-slate-600">@(!string.IsNullOrEmpty(t.RelatedAccountNumber) && nameMap.ContainsKey(t.RelatedAccountNumber) ? nameMap[t.RelatedAccountNumber] : "—")</td>
                        <td class="px-4 py-3 text-right text-sm font-semibold text-slate-900">@t.Amount.ToString("C")</td>
                        <td class="px-4 py-3 text-sm text-slate-600">@t.Date.ToLocalTime().ToDisplayDateTimeSeconds()</td>
                        <td class="px-4 py-3"><span class="inline-flex items-center rounded-full px-2.5 py-0.5 text-xs font-medium @(t.Status == "Completed" ? "bg-emerald-100 text-emerald-800" : "bg-amber-100 text-amber-800")">@t.Status</span></td>
                        <td class="px-4 py-3 text-sm text-slate-600">@t.Note</td>
                        <td class="px-4 py-3">
                            <a asp-controller="Admin" asp-action="TransactionDetails" asp-route-id="@t.Id"
                               class="rounded-md bg-slate-100 px-2.5 py-1 text-xs font-medium text-slate-700 hover:bg-slate-200">View</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

    <partial name="_Pagination" />
</div>